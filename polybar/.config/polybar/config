[colors]
background = #00000000
foreground = #fff
primary = #ffb52a
secondary = #e60053
alert = #bd2c40

[bar/main]
width = 100%
height = 35
radius = 0
fixed-center = true
offset-y = 0
wm-restack = bspwm

background = ${colors.background}
foreground = ${colors.foreground}

line-size = 0
border-size = 2
border-bottom-color = #22000000

padding-left = 3
padding-right = 3
module-margin-left = 1
module-margin-right = 1

override-redirect = true

font-0 = FiraCode Nerd Font Mono:size=20;6
font-1 = FiraCode Nerd Font Mono:size=20;6
font-2 = FiraCode Nerd Font Mono:size=20;6

modules-left = bspwm
modules-center =
modules-right = lan battery date

tray-position = right
tray-margin-top = 10

cursor-click = pointer
cursor-scroll = ns-resize

[module/bspwm]
type = internal/bspwm

label-focused = %name%
label-occupied = %name%
label-urgent = %name%
label-empty = %name%

label-separator = " "
label-separator-padding = 0

label-focused-foreground = #000
label-focused-background = #fff

label-occupied-foreground = #fff
label-occupied-background = #000

label-empty-foreground = #444
label-empty-background = #000

[module/xbacklight]
type = internal/xbacklight

format = <label> <bar>
label = 

bar-width = 10
bar-indicator = |
bar-indicator-foreground = ${colors.foreground}
bar-indicator-font = 2
bar-fill = ─
bar-fill-font = 2
bar-fill-foreground = #228a91
bar-empty = ─
bar-empty-font = 2
bar-empty-foreground = ${colors.foreground}

[module/lan]
type = internal/network
interface = wlp3s0
interval = 3.0

format-connected = <label-connected>
label-connected = %local_ip%

format-disconnected = WLAN down
format-disconnected-foreground = #ff0

format-connected-prefix = "WLAN "
format-connected-prefix-foreground = #0f0

[module/date]
type = internal/date
interval = 5

date =
date-alt = " %Y-%m-%d"

time = %H:%M
time-alt = %H:%M:%S

label = %date% %time%

[module/alsa]
type = internal/alsa

format-volume = <label-volume> <bar-volume>
label-volume = 墳
label-volume-foreground = ${root.foreground}

format-muted-foreground = ${colors.foreground}
label-muted = ﱝ

bar-volume-width = 10
bar-volume-indicator = |
bar-volume-indicator-font = 2
bar-volume-fill = ─
bar-volume-fill-font = 2
bar-volume-fill-foreground = #228a91
bar-volume-empty = ─
bar-volume-empty-font = 2
bar-volume-empty-foreground = ${colors.foreground}

[module/battery]
type = internal/battery
battery = BAT0
adapter = AC
full-at = 98
low-at = 8

format-charging = <label-charging>
format-discharging = <label-discharging>
format-full = FULL
format-full-foreground = #0ff

format-low = <animation-low> <label-discharging>

format-charging-prefix = "CHG "
format-charging-prefix-foreground = #0f0

format-discharging-prefix = "DIS "
format-discharging-prefix-foreground = #ff0

animation-low-0-foreground = #f00
animation-low-0 = CRIT

animation-low-1-foreground = #300
animation-low-1 = CRIT

animation-low-framerate = 300

[settings]
screenchange-reload = true

; vim:ft=dosini
